<div class="under">
  <nav class="navbar navbar-expand-lg navbar-light bg-light  bg-white rounded" id = "nav-top">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="nav navbar-nav ml-auto">
        <li class="nav-item _n01">
          <a class="nav-link link-color" href="#">Acceuil <span class="sr-only">(current)</span></a>
        </li>

        <% if current_user %>
          <li class="nav-item _n01">
            <a class="nav-link link-color" href="#">Actualités</a>
          </li>
        <% end %>

        <li class="nav-item _n01">
          <a class="nav-link link-color" href="#">Projets</a>
        </li>

        <% if current_user %>
          <li class="nav-item _n01">
            <a class="nav-link link-color" href="#">Notification<span class="badge badge-primary pos_badge">4</span></a>
          </li>
        <% end %>

        <% if current_user %>
          <li class="nav-item _n01">
            <a class="nav-link link-color" href="#">Bons plans<span class="badge badge-primary pos_badge">10</span></a>
          </li>
        <% end %>

        <% if current_user %>
          <li class="nav-item _n01">
            <a class="nav-link link-color" href="#">Takalo</a>
          </li>
        <% end %>
        <li class="dropdown nav-item _trans-late _n01">
          <a href="#" class="dropdown-toggle link-color" data-toggle="dropdown">
            <% if current_user %>
              <%if current_user.avatar.attached? %>
                <%= image_tag current_user.avatar , class:'profile-image img-circle'%> <%= current_user.name %>
              <%  else %>
                
              <%end%>
            <% else %>
              <span>connection</span>
            <% end %>
            <b class="caret"></b>
          </a>
            <ul class="dropdown-menu nav-translate">
              <% if current_user %>
                <li class="p0"><a href="#" class="nav-link">Account</a></li>
                <li class="divider"></li>
                <li class="p0"><a href="#" class="nav-link">liens d'em</a></li>
                <li class="divider"></li>
                <li class="p0"><%= link_to "Sign out", destroy_user_session_path, :method => :delete , class:"nav-link"%></li>
              <% else %>
                <li class="p0"><a class="nav-link c0" href="/users/sign_in">Sign in</a></li>
                <li class="p0"><a class="nav-link c0" href="/users/sign_up">Sign up</a></li>
              <% end %>
            </ul>
        </li>
      </ul>
    </div>
  </nav>
</div>

<%if current_user%>
<%end%>